# 验证记录 — 2026-09-10

- 真实页面只读检查：英特尔官方旗舰店商品管理价格弹窗，表头为配置、价格、到手价、SKUID、商家编码、预览图、SKU状态。真实 SKU ID 与行 data-row-key 不同。
- 虚拟滚动容器实查：clientHeight 428、scrollHeight 1830；初始渲染 6 行，行覆盖区间 0～540。仅取初始 DOM 不完整，因此实现以滚动窗口覆盖区间逐段校验。
- `npm test`：19 项通过、0 失败。包括输入和金额、真实 ID/名称核对、重复目标价格冲突、20 行虚拟列表、首尾标价修改、停滞/漏行、SKU 与行 key 一对一校验、安装文件完整面板流程、保存异常/回读异常、切店/停止/旧价变化及原价相同跳过。
- `pretest` 自动构建当前源代码，集成测试直接运行生成的 user.js；语法检查通过。
- 独立审查未发现新增的重要可复现错写或错误成功问题。审查发现的旧构建测试风险已通过 pretest 修正；主 Agent 发现的重复真实 SKU 映射已添加回归测试并修正。
- 没有安装到用户扩展，没有保存任何真实商品标价。模拟页面保存并不能证明后台持久化；真实生产端到端验证仍需安装后使用目标清单执行。
