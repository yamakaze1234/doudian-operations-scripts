# 验证记录 — 2026-09-10

- `npm test`：20 项通过、0 失败。覆盖 SPU 字符串保真、简称严格匹配、歧义/重复识别、库存结构和合计、现货及多档预售清零、非目标保留、库存变化/切店/停止、保存异常不重试、原零库存跳过，以及操作面板完整模拟流程。
- `npm run build` 与 `node --check 抖店库存调整自动化.user.js`：通过。
- 独立只读代码审查：未发现有充分证据支持的误改 SKU、自动重试保存或错误成功声明问题；独立复跑 20 项测试通过。
- 真实页面只读核验：英特尔官方旗舰店，商品 `3796634191500804129`；库存窗口 20 条 SKU，现货与预售明细合计 115，商品总库存 115；全部 SKU 名称可读取，取消和保存按钮在库存抽屉内部。
- 没有提交真实库存改动，没有安装到用户扩展。

本地集成测试中的保存是模拟页面行为，不能证明真实后台受控输入组件接受事件或服务器持久化完成。生产端到端验收尚未完成；只有使用者执行后，工具重新打开库存窗口并验证通过的记录才标为“已验证清零”。

